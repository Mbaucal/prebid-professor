name: Patch ads.txt variable records

on:
  push:
    branches:
      - feature/ads-txt-duplicate-requirements-v1
    paths:
      - .github/patch-ads-txt-variables.trigger

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feature/ads-txt-duplicate-requirements-v1
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Add ads.txt variable support
        run: python3 .github/scripts/patch_ads_txt_variables.py

      - name: Install dependencies
        run: npm ci

      - name: Build dashboard and Worker
        run: npm run build

      - name: Check patch formatting
        run: git diff --check

      - name: Remove one-time integration files
        run: |
          rm -f .github/scripts/patch_ads_txt_variables.py
          rm -f .github/workflows/patch-ads-txt-variables.yml
          rm -f .github/patch-ads-txt-variables.trigger

      - name: Commit ads.txt variable support
        run: |
          git config user.name "Tessera Ads.txt"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Support ads.txt variable declarations" || exit 0
          git push origin HEAD:feature/ads-txt-duplicate-requirements-v1

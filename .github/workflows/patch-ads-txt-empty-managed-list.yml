name: Patch ads.txt empty managed list

on:
  push:
    branches:
      - feature/ads-txt-duplicate-requirements-v1
    paths:
      - .github/patch-ads-txt-empty-managed-list.trigger

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feature/ads-txt-duplicate-requirements-v1
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply empty-list fix
        run: python3 .github/scripts/patch_ads_txt_empty_managed_list.py

      - name: Install dependencies
        run: npm ci

      - name: Build dashboard and Worker
        run: npm run build

      - name: Check patch formatting
        run: git diff --check

      - name: Remove one-time patch files
        run: |
          rm -f .github/scripts/patch_ads_txt_empty_managed_list.py
          rm -f .github/workflows/patch-ads-txt-empty-managed-list.yml
          rm -f .github/patch-ads-txt-empty-managed-list.trigger

      - name: Commit fix
        run: |
          git config user.name "Tessera Ads.txt"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Show live add flow with an empty managed list" || exit 0
          git push origin HEAD:feature/ads-txt-duplicate-requirements-v1
